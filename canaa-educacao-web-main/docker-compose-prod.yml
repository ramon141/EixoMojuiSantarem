version: '3.7'
services:
  web-prod:
    image: canaa-educacao-web:prod
    container_name: canaa-educacao-web-prod
    build:
      context: .
      dockerfile: Dockerfile-prod
    ports:
      - '${PORT}:80'
    env_file:
      - .env